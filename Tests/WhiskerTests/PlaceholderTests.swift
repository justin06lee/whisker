import XCTest

// Placeholder test so the WhiskerTests target path resolves and `swift build`
// succeeds. Real tests are added in later tasks.
final class PlaceholderTests: XCTestCase {
    func testPlaceholder() {
        XCTAssertTrue(true)
    }
}
